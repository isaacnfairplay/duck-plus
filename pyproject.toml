[project]
name = "duck-plus"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.3.1",
]

[dependency-groups]
dev = [
    "pytest>=8.4.1",
]
